-- Intentionally empty.
--
-- Production identities, administrator memberships, catalog values, stories,
-- subscriptions, and favorites must never be represented by dummy seed rows.
-- Add environment-specific fixtures in a separate, untracked local seed file
-- when integration tests require them.

